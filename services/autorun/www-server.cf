# -*-cfengine-*-
#
# Time-stamp: <2019-01-29 15:23:33 nsa>
#
# garantera apache
#

bundle agent www_server
{
  meta:
      "tags" slist => { "autorun" };

  packages:
    WWW_services::
      "apache2-bin"
	policy => "present",
	classes => results("bundle", "apache_installation");

  services:
    !any.WWW_services::
      "isc-dhcp-server"
	service_policy => "enable",
	ifvarclass => "apache_installation_repaired";

}
